/* added by kinit-standalone
 *
 * the only purpose is to make sure progname is defined only here
 * so that it does not get linked into the lib which would conflict
 * with kinit (which defines its own)
 */

const char *progname = (void *)0;

